{
  "name": "my-dictionary",
  "version": "1.0.0",
  "description": "My-Dictionary is a web application that users can search the definition of a word using [Merriam-Webster API](https://www.merriam-webster.com/).",
  "scripts": {
    "prepare-client": "cd client && yarn install && yarn run build",
    "prepare-server": "cd server && yarn install",
    "heroku-postbuild": "yarn run prepare-client && yarn run prepare-server",
    "start": "cd server && yarn start"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hyunse/my-dictionary.git"
  },
  "author": "Hyunse Kim",
  "license": "ISC"
}
